## Input, output, and reference file paths

# Directory containing raw FASTQ files
raw_fastq_dir: "/home/eherman/scratch/Stothard/FERTILITY/variant_calling/raw_fastq_test/"
# Directory containing the reference genome, bwa indices, and known variants (VCF) files
reference_dir: "/home/eherman/scratch/Stothard/FERTILITY/variant_calling/reference_files/"
# Path to the basename of the reference genome file (no .fa extension)
reference: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/reference_files/ARS-UCD1.2_Btau5.0.1Y
# Output directory - all output files will be created within subdirectories of the final dir in this path
output_dir: "/home/eherman/scratch/Stothard/FERTILITY/variant_calling/mapping/"
# File containing a list of contig names (not chromosome names)
contigs_names_file: '/home/eherman/scratch/Stothard/FERTILITY/variant_calling/ARS-UCD1.2_Btau5.0.1Y.fa.contigsNAMES.txt'
# File containing known SNPs (can be vcf or bed)
dbsnp: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/reference_files/ARS1.2PlusY_BQSR.vcf.gz
# Adapters file for QC trimming
adapters:
        trimmomatic: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/TruSeq3-PE-2_NexteraPE-PE.fa


## Paths to program executables

java: java
java_1p8: /cvmfs/soft.computecanada.ca/easybuild/software/2017/Core/java/1.8.0_192/bin/java
bwa: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/bins/bwa
fastqc: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/bins/FastQC/fastqc
trimmomatic: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/bins/trimmomatic-0.38.jar
samtools: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/bins/samtools
picard: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/bins/picard.jar
bam_checker: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/bins/bam_checker.py
gatk: /home/eherman/scratch/Stothard/FERTILITY/variant_calling/bins/GenomeAnalysisTK.jar

## Program parameters

# Number of chunks to split raw fastq files into (files should be smaller than 100M reads)
n_of_chunks: 2
# Each read file contains R1 or R2 in the filenames
read_index: ['R1', 'R2']
# This value is 100 for data generated on non-arrayed flowcells (ie from GAIIx, HiSeq1500/2000/2500), or 2500 for arrayed flowcell data (eg HiSeqX, HiSeq3000/4000, NovaSeq). Note these are all Illumina instruments, if you have data from other instruments you must work with the supplier to determine this value. See https://sequencing.qcfail.com/articles/illumina-patterned-flow-cells-generate-duplicated-sequences/
optical_duplicate_pixel_distance: 2500
# Type of file containing known SNPs (can be vcf or bed)
dbsnp_format: vcf
# Number of contigs to process per job for variant calling
n_of_contigs: 100
# Chromosome information
autosomes_range_first: 1
autosomes_range_last: 29
other_chrs: ['X', 'Y', 'MT']
# Internal value, do not change
chr_prefix: ''

## Thread and Java memory parameters - any changes made here must also be made
## in the corresponding rules in the clusterconfig.yaml file, to avoid
## disagreements between requested and available memory.

threads:
        def: 1
        med: 4
        big: 30

java_mem:
        def: -Xmx7500m
        med: -Xmx30g
        big: -Xmx100g
